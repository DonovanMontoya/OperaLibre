# Folder containing your audiobook files.
# A folder with multiple tracks is treated as one book.
AUDIOBOOK_LIBRARY=/path/to/audiobooks

# Server configuration.
PORT=4000
HOST=0.0.0.0

# Used by the web app when it is not served through the Vite dev proxy.
VITE_API_BASE=http://localhost:4000
